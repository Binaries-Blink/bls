/// single line function, no block needed
fn calc(n: u32) u32 = (n + 3) * 8

/// function with a block body
fn calc2(n: u32) u32 = {
    ret (n + 3) * 8;
}

let x = calc(4);
let y = calc2(5) / 2;
// would require an explicit cast if z were of type  u64
let z: u32 = x * y;
