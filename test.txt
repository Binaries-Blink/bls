/// single line function, no block needed
// todo : blast need support for unary ops other than '-', like '~' and '+'
fn calc(n: u64) u64 = (n + 3) * --8

let x = calc(2);      // 40
let y = calc(x / 2);  // 184

// todo : if we remove the else block we
// get a stack overflow,
// it should simply error out lol,
// upon further reflection,
// this issue should be addressed in blast,
// as it seems like a semantic error.
let z = y * if (x == 40) x else y;

// todo : support returning from main chunk